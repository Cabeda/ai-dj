"""Test suite.

Note: `python3 -m unittest discover -s tests` imports these modules top-level,
so this file does not run. Silence is the default of
`ai_dj.backend.make_strudel_backend` instead — the app opts *in* to sound — so
the suite never schedules audio to the speakers.
"""
